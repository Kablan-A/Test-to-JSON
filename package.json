{
  "name": "test_to_json",
  "version": "1.0.0",
  "description": "Test to JSON. Converts a test to a JSON",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kablan",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "serverless-http": "^3.2.0"
  }
}
